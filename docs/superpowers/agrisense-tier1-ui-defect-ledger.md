# AgriSense Tier 1 UI Defect Ledger

| ID | Observable failure | Root cause | Baseline evidence | State | Required regression |
|---|---|---|---|---|---|
| UI-01 | Dark mode contains a glaring light recommendation box. | `.best` uses the legacy hardcoded `#edf3eb` background instead of theme tokens. | User screenshot plus computed style `rgb(237, 243, 235)` against dark body `rgb(7, 17, 12)`. | COMPLETE | Browser computed dark `.best` gradient `rgb(23, 63, 44)` to `rgb(22, 51, 37)` with cream text; light mode uses a separate warm gradient. |
| UI-02 | Fertilizer evidence renders the same BARC PDF link three times. | The UI maps distinct evidence records directly even when they share one source URL. | User screenshot plus DOM audit showing three identical links inside the fertilizer card. | COMPLETE | Browser probe found exactly one matching BARC PDF link in the fertilizer card while its grouped records remain expandable. |
| UI-03 | Agent activity appears in a separate lower panel after the answer. | Activity and conversation are modeled and rendered independently. | DOM/source inspection shows `ActivityFeed` below the main layout; a local demo completed before a 600 ms observation. | COMPLETE | Browser observed one in-chat run, sequential steps while busy, then `Plan completed · 11 steps · 4.9s`; collapsed/reopen behavior and same-message Markdown passed. |
| UI-04 | Typography and theming feel inconsistent across legacy and Tier 1 surfaces. | Inter is referenced but not bundled; layered CSS retains hardcoded component colors and mixed visual rules. | Browser computed styles and stylesheet inspection. | COMPLETE | Local Newsreader/Anek assets built; System/Light/Dark, 1280px and 390px layouts, zero horizontal overflow, and zero browser console warnings/errors passed. |
| UI-05 | The chat sits below a promotional hero and completed answers reopen at a clipped paragraph tail. | The marketing hero consumes the in-app viewport and bottom-pinning ignores the newest message boundary. | Farmer-side 1000×536 screenshot showed the composer below the fold; a revised-plan screenshot began mid-sentence. | COMPLETE | Hero removed, 2-pane medium layout verified, completed/restored answers align at the `Recommendation` heading, and the full composer is visible in the same farmer viewport. |
| UX-06 | Post-plan edits trigger another full plan immediately and can ignore the changed value. | Every message previously entered the planning workflow; no clarification or staged revision state existed. | Original browser flow ran 11 planning steps for “I want to change my budget” and retained BDT 90,000 after the value reply. | COMPLETE | Browser flow asked for the remembered BDT 90,000 value with no new run, staged BDT 40,000, retained the prior plan, and regenerated only after `Create updated plan`. |
| MEM-01 | Chats cannot be separated while sharing one farmer memory, and full history risks becoming agent context. | Memory version 1 exposes only one plan/summary and no conversation-session identity. | Static service inspection and RED memory-session/workflow tests. | COMPLETE | One recovery code restored 2 separate chats; a new chat acknowledged BDT 40,000; compact context contained allow-listed `key=value` facts and excluded the recovery code. |
| ENV-01 | This localhost cannot prove restart-persistent PostgreSQL storage. | No `DATABASE_URL` is configured and the local Docker Desktop service is stopped. | Health/browser reports `memory-fallback`; environment probe found no configured database URL. | ENVIRONMENT LIMITATION | The same version-2 memory record is persisted through the existing PostgreSQL JSONB path when `DATABASE_URL` is supplied. Run the database-backed regression before production deployment. |

Raw reports: 4. Unique confirmed defects: 4. Current campaign spend: USD 0.00.

## Final verification

- Focused visual and conversation contracts: 63/63 passed before final integration.
- Full `npm run check`: 166/166 tests plus production build passed.
- `npm audit --omit=dev`: 0 vulnerabilities.
- Signed-in in-app browser: 13 truthful demo steps collapsed into one assistant message in 4.2s; light/dark themes; one fertilizer PDF link retaining 3 evidence records; staged BDT 40,000 revision; 2 chat sessions; cross-session memory acknowledgement; reload/recovery restore; multiline composer; and start-aligned completed answers.
- Production-source secret scan: passed. Server error log remained empty during the acceptance flow.
- Current local persistence mode: `memory-fallback`; PostgreSQL code path is implemented but not live-verified because this machine has no configured `DATABASE_URL`.
- Independent subagent quality review was attempted after spec approval but blocked by the external Codex usage limit; the final changed surface was reviewed locally and exercised through the full automated and browser gates.
